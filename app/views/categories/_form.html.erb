
<%= form_for @category, class: "form-horizantal" do |f| %>
<div class="form-group">
  <%= f.label :title, class: "col-lg-4 control-label" %>
  <%= f.text_field :title, class: "form-control" %>
</div>

  <%= f.submit "Update", class: "btn btn-primary btn-block"%>
  <% end %>
