

<%= form_for (@company), class: "form-horizantal" do |f| %>
  <%= f.label :name,  class: "col-lg-4 control-label" %>
  <%= f.text_field :name,  class: "form-control" %>

  <%= f.submit "Update", class: "btn btn-primary btn-block" %>
<% end %>
